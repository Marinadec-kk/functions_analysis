# ========================================
# CORE SETTINGS
# ========================================

# Input folder containing .docx documents to process
INPUT_FOLDER=input_documents

# Output Excel file for analysis results  
OUTPUT_EXCEL=data/analysis_results.xlsx

# Output folder for generated markdown documentation
OUTPUT_MARKDOWN_FOLDER=data/markdown_output

# Reference files (should point to your existing data)
SPHERES_FILE=files/SPHERES.xlsx
UNIVERSAL_FUNCTIONS_JSON=files/universal_functions.json

# ========================================
# AI CONFIGURATION
# ========================================

# AI mode: 'online' for OpenAI API, 'local' for local server, 'alternative' for alternative provider
AI_MODE=local

# AI model for chat completions
AI_MODEL=gemma-3-27b-it

# For local/alternative mode: URL to your vLLM/LM Studio/Ollama server for AI inference
# Example: http://localhost:7010 (for vLLM server running on port 7010)
EMBEDDING_SERVER=http://localhost:7010

# Embedding model configuration
# When USE_LOCAL_EMBEDDINGS=true, this should be a HuggingFace model name
# When USE_LOCAL_EMBEDDINGS=false, this should be the model name served by EMBEDDING_SERVER
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-mpnet-base-v2

# Use local GPU for embeddings via sentence-transformers (recommended for local mode)
# Set to 'true' to load embeddings model directly with transformers (no separate server needed)
# Set to 'false' to use EMBEDDING_SERVER for embeddings via API calls
USE_LOCAL_EMBEDDINGS=true

# For online mode (OpenAI API) - uncomment and set your API key
# OPENAI_API_KEY=sk-...

# ========================================
# AI MODEL PARAMETERS
# ========================================

# Temperature (0.0-2.0): Controls randomness. Higher = more creative, Lower = more deterministic
# Recommended: 0.3-0.7 for classification tasks, 1.0 for balanced responses
AI_TEMPERATURE=1.0

# Max tokens for completion
# Recommended: 2000-4000 for government function analysis
AI_MAX_TOKENS=4000

# ========================================
# PROCESSING PARAMETERS
# ========================================

# Similarity threshold for collision detection (0.0-1.0)
SIMILARITY_THRESHOLD=0.5

# Universal similarity threshold for suggestions (0.0-1.0)  
UNIVERSAL_SIMILARITY_THRESHOLD=0.75

# Performance settings (currently not used, reserved for future parallelization)
EMBEDDING_WORKERS=8
AI_WORKERS=30
BATCH_SIZE=64

# ========================================
# LOGGING
# ========================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
